<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlyJobs">
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="onlyjobs.firebaseapp.com"
                    android:pathPrefix="/__/auth/links" />
            </intent-filter>

        </activity>


        <activity android:name=".LearnMoreActivity"
            android:exported="false"/>
        <activity
            android:name=".StudentAuthActivity"
            android:exported="true">
        </activity>

        <activity android:name=".RolesActivity" />
        <activity android:name=".EmployerAuthActivity" />
        <activity android:name=".StudentDashboardActivity" />
        <activity android:name=".EmployerDashboardActivity" />
    </application>


</manifest>
